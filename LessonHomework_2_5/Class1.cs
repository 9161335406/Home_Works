using System;

internal class Winter
{
    public static string Number { get; private set; }
    public Temp(string month, decimal temp)
    {
        static int GetRandom()
        Random rnd = new Random();

        int temp = rnd.Next(-5, 5);

        switch (number)
        {
            case 1:
                month = "December";
                return temp;

                break;

            case 2:
                month = "Ynuary";
                return temp;

                break;

            case 3:
                month = "February";
                return temp;

                break;

            default:
                month = "Месяц не зимний";

                break;
        }
        if (temp > 0 && (number <= 1 || number >= 3);
        {
            return += " " + "Дождливая зима";
        }

    }
}